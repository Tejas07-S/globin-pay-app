# Local development. For web / iOS simulator / Android emulator, localhost works.
# For a PHYSICAL device via Expo Go, replace localhost with your computer's
# LAN IP (e.g. http://192.168.1.42:8000) — the phone can't resolve "localhost"
# to your dev machine.
EXPO_PUBLIC_BACKEND_URL=http://localhost:8000
